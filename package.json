{
  "name": "auto-glaucoma-triage",
  "version": "1.0.0",
  "description": "Automated Glaucoma Referral Triage System using Gemini AI",
  "scripts": {
    "dev": "wrangler pages dev --compatibility-date=2023-06-21 --compatibility-flag=nodejs_compat",
    "build": "mkdir -p dist && cp -r public/* dist/ && cd functions && npm install"
  },
  "dependencies": {
    "@google/generative-ai": "^0.2.0",
    "dotenv": "^16.3.1",
    "node-fetch": "^2.7.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  },
  "engines": {
    "node": ">=16.13.0"
  }
}
